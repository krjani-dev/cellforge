# cellforge

A React spreadsheet package — pre-alpha. Public API and documentation will land alongside the first published release.

`npm install cellforge` will resolve once `v1.0.0` ships. Until then, releases publish under explicit dist-tags (`@dev`, `@alpha`, `@beta`, `@next`).

## License

[MIT](./LICENSE)
