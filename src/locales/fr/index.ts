// cellforge/locales/fr — French string catalog. Pure JSON-equivalent export;
// no extra dependencies.

export const locale = 'fr' as const;

export const messages: Record<string, string> = {
  // Toolbar / menu / dialog strings populate as features land.
};

export default { locale, messages };
